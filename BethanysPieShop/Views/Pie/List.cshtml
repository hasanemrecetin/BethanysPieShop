@model BethanysPieShop.Models.ViewModels.PiesListViewModel

<h1>@Model.CurrentCategory</h1>
@foreach (var pie in Model.Pies)
{
    <div>
        <h2>@pie.Name</h2>
        <h3>@pie.Price.ToString("c")</h3>
        @if (pie.Category != null)
        {
        <h4>@pie.Category.CategoryName</h4>
        }
        else
        {
        <h4>Not found!</h4>
        }
    </div>
}